import styled from 'styled-components';

export const SCoinListWrapper = styled.div`
  border-radius: 4px;
  padding: 8px 8px 0 8px;
  height: 100%;
  overflow: auto;
  box-shadow: rgba(0, 0, 0, 0.09) 0 3px 12px;
`;
